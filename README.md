# customer_lifetime_value
Engineer RFM features from raw transactions, predict 90-day CLV with XGBoost, segment customers with KMeans, store embeddings in pgvector, and run live similarity queries — all interpreted through a business lens.
